# Cache Puppeteer's Chrome inside node_modules so Vercel's build cache picks it
# up between deploys (avoids re-downloading the ~170 MB Chrome binary on every
# build). Read by puppeteer >= 19.
puppeteer_cache_dir=./node_modules/.cache/puppeteer
